---
"@neomaventures/mockserver": minor
---

Add `@neomaventures/mockserver/fixture` subpath export — a singleton `mockserver` client that auto-resets between tests via a `beforeEach` hook. Reads `MOCKSERVER_URL` from the environment.
