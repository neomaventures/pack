---
"@neoma/fixtures": minor
---

Add `multerFile()` — a faker-populated `Express.Multer.File` mock for file-upload tests. Top-level export, re-exported from the package root. (Promoted from `@neoma/cerberus`'s local fixtures.)
