NODE_ENV=specs

# Container input ports (healthcheck slot specs sub-range, 4100)
NEOMA_TEST_PREFIX=healthcheck-specs
MOCKSERVER_PORT=4100

# Container-derived connection details
MOCKSERVER_URL=http://localhost:4100/mockserver
